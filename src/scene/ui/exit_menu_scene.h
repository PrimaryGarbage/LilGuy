#ifndef __EXIT_MENU_SCENE_H__
#define __EXIT_MENU_SCENE_H__

#include "scene/scene.h"

// Returns true if the application should close
Scene* ExitMenuScene_Create(Scene* parent);

#endif // __EXIT_MENU_SCENE_H__